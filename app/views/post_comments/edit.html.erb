<h2>Comment Edit</h2>

<%= form_for [@book, @comment] do |f| %>
	<div class="row">
		<div class="col-sm-6">
			<%= f.text_area :comment, class: "form-control",placeholder: "コメントをここに入力してください" %>
		</div>
	</div>
	<%= f.submit "push", class: "btn btn-base-1 mt-20 pull-left" %>
<% end %>